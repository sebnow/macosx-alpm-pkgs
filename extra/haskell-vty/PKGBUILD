# Contributor: Sebastian Nowicki <sebnow@gmail.com
# This package is based on the Archlinux PKGBUILD, written by:
# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.4.2
pkgname=haskell-vty
pkgrel=1
pkgver=3.1.8
pkgdesc="A simple terminal access library"
url="http://hackage.haskell.org/cgi-bin/hackage-scripts/package/vty"
license=('custom:BSD3')
arch=('i386' 'x86_64')
depends=('ghc' 'haskell-cabal' 'haskell-terminfo<=3.0.0' 'haskell-utf8-string')
source=("http://hackage.haskell.org/packages/archive/vty/$pkgver/vty-$pkgver.tar.gz")
install=haskell-vty.install
md5sums=('023b2c1d371fc257614dffdce9565860')
build() {
  cd "$srcdir/vty-$pkgver"
  runhaskell Setup configure --prefix=/usr/local || return 1
  runhaskell Setup build || return 1
  runhaskell Setup register --gen-script || return 1
  runhaskell Setup unregister --gen-script || return 1
  mkdir -p "$pkgdir/usr/local/share/haskell/$pkgname/"
  install -m744 register.sh "$pkgdir/usr/local/share/haskell/$pkgname/register.sh"
  install -m744 unregister.sh "$pkgdir/usr/local/share/haskell/$pkgname/unregister.sh"
  runhaskell Setup copy --destdir="$pkgdir" || return 1
  mkdir -p "$pkgdir/usr/local/share/licenses/$pkgname/"
  install -m644 LICENSE "$pkgdir/usr/local/share/licenses/$pkgname/LICENSE" || return 1
}
