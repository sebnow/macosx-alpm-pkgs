# Contributor: Sebastian Nowicki <sebnow@gmail.com
# This package is based on the Archlinux PKGBUILD, written by:
# Contributor: Arch Haskell Team <arch-haskell@haskell.org>
# Package generated by cabal2arch 0.4.4
pkgname=haskell-regex-tdfa
pkgrel=1
pkgver=0.97.3
pkgdesc="Accurate POSIX extended regular expression library"
url="http://hackage.haskell.org/cgi-bin/hackage-scripts/package/regex-tdfa"
license=('custom:BSD3')
arch=('i386' 'x86_64')
depends=('ghc' 'haskell-cabal' 'haskell-regex-base>=0.80', 'haskell-parsec')
source=("http://hackage.haskell.org/packages/archive/regex-tdfa/$pkgver/regex-tdfa-$pkgver.tar.gz")
install=haskell-regex-tdfa.install
md5sums=('29bb22acc1c9c4684940aeb53483ee71')
build() {
  cd "$srcdir/regex-tdfa-$pkgver"
  runhaskell Setup configure --prefix=/usr/local || return 1
  runhaskell Setup build || return 1
  runhaskell Setup register --gen-script || return 1
  runhaskell Setup unregister --gen-script || return 1
  mkdir -p "$pkgdir/usr/local/share/haskell/$pkgname/"
  install -m744 register.sh "$pkgdir/usr/local/share/haskell/$pkgname/register.sh"
  install -m744 unregister.sh "$pkgdir/usr/local/share/haskell/$pkgname/unregister.sh"
  runhaskell Setup copy --destdir="$pkgdir" || return 1
  mkdir -p "$pkgdir/usr/local/share/licenses/$pkgname/"
  install -m644 LICENSE "$pkgdir/usr/local/share/licenses/$pkgname/LICENSE" || return 1
}
